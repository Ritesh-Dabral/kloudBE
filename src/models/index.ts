/**
 * All model imports
 */

    import {FileModel} from './files';
    import {UserModel} from './user';
    import {VerifMailModel} from './verificationMail';

    const AllModels = {
        FileModel,
        UserModel,
        VerifMailModel
    }

    export default AllModels;